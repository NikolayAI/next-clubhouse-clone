import { createGate } from 'effector-react/compat';import { sample } from 'effector/compat';import { setGitHubSession } from './helpers';export const EnterInfoPageGate = createGate();sample({  clock: EnterInfoPageGate.open,  fn: () => {    window.addEventListener('message', setGitHubSession);  },});sample({  clock: EnterInfoPageGate.close,  fn: () => {    window.removeEventListener('message', setGitHubSession);  },});