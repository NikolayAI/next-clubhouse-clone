import { createGate } from 'effector-react/compat';import { sample } from 'effector/compat';import { setUser } from '../../entities/user';export const EnterInfoPageGate = createGate();sample({  clock: EnterInfoPageGate.open,  fn: () => {    window.addEventListener('message', setUser);  },});