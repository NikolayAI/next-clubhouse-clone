import { createEffect, createEvent, createStore } from 'effector/compat';import { IAuthResponse } from '../../../shared/api';const $session = createStore<IAuthResponse>({} as IAuthResponse);const setGitHubSession = createEvent<IAuthResponse>();const authGitHubFx = createEffect<void, void, Error>();authGitHubFx.use(async () => {  window.open(    'http://localhost:3001/auth/github',    'Auth',    'width=500,height=500,status=yes,toolbar=no,menubar=no,location=no',  );});$session.on(setGitHubSession, (_, session) => session);$session.watch(data => console.log('SESSION: ', data));export const events = {  setGitHubSession,};export const effects = {  authGitHubFx,};export const stores = {  $session,};