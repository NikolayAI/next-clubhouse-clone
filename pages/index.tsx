import type {NextPage} from 'next';

import {GlobalStyle} from '../shared/styles/globalStyles';

const Home: NextPage = () => {
  return (
    <div>
      <GlobalStyle/>
    </div>
  );
};

export default Home;
